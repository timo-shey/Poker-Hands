package card;

/**
 * Enum representing the four suits in a standard deck of playing cards.
 * Each suit corresponds to a specific symbol.
 */
public enum Suit {
    SPADES, HEARTS, CLUBS, DIAMONDS
}
